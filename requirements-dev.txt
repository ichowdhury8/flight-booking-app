# Test-only dependencies. Deliberately NOT in requirements.txt — Render installs
# that file on every deploy and has no reason to build a test runner.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest -q

-r requirements.txt
pytest==9.0.1
httpx==0.28.1
