# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/

# Database — regenerated by the startup seed on every boot (see PLAN.md R1)
flights.db

# macOS
.DS_Store

# NOTE: static/ is deliberately NOT ignored.
# The Vite build output is committed to git so that Render's build command can
# stay Python-only and never depends on Node being present in the build image.
# See PLAN.md R3 and R9. Do not add static/ here.
